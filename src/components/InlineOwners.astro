---
interface Props {
  owner: string | undefined;
  url: string;
}

const { owner } = Astro.props;
const { url } = Astro.props;
---

<a href={url} class="hover:underline">
  {owner || "Default Owner"}
</a>
